# Backend API base URL (inlined at build time)
NEXT_PUBLIC_API_URL=http://localhost:5000/api

# Base URL for legacy property image files (DB stores filenames only)
# Change to /uploads/properties to serve from the local backend instead
NEXT_PUBLIC_PROPERTY_IMAGE_BASE=https://amaken-realestate.com/admin/property